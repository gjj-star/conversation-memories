# 内网穿透（NAT 穿透）

## 是什么

内网穿透是让**外网设备能够访问内网（局域网）服务**的技术。

**为什么需要**：家庭/公司网络通常在 NAT（网络地址转换）路由器后面，没有公网 IP——外网无法直接访问你的电脑。内网穿透通过一台有公网 IP 的中转服务器建立隧道，让流量绕进去。

## 核心原理

```
你的电脑（内网）           中转服务器（公网IP）          外部用户
  192.168.1.5    ──主动连接──→   frp.example.com  ←──访问──  任意设备
                                   ↑
                    你的电脑主动建立的长连接隧道
                    外部流量经隧道转发到你的电脑
```

**关键点**：内网设备**主动**连接中转服务器建立长连接，流量通过这条已建立的隧道"逆流"进入内网——不依赖内网有公网 IP 或被外网直接访问。

## 常用工具

| 工具 | 特点 | 适用场景 |
|------|------|----------|
| **frp**（Fast Reverse Proxy） | 开源、自部署、功能全、支持 TCP/UDP/HTTP/HTTPS | 自建服务、企业级 |
| **ngrok** | 一条命令即刻穿透、免费版有域名限制和限速 | 开发调试、临时演示 |
| **Cloudflare Tunnel**（Argo） | 免费、自带 CDN/DDoS 防护、不需要公网服务器 | 个人服务、静态站点 |
| **ZeroTier / Tailscale** | P2P 虚拟组网，不依赖中转 | 远程办公、设备互联 |

## 安全注意事项

- **不要穿透到生产数据库**：内网穿透等于在内网防火墙上打了个洞
- **认证和加密**：至少加 HTTP Basic Auth + TLS，不要裸奔
- **最小权限**：只暴露需要的端口和服务
- **监控异常流量**：穿透隧道是攻击者的潜在入口

## 典型使用场景

1. **本地开发调试 Webhook**：把本地服务暴露给第三方回调
2. **远程访问 NAS/家庭服务器**
3. **临时演示**：给客户看本地跑的项目
4. **远程桌面/SSH**：从公司访问家里电脑

## 一句话总结

内网穿透 = 内网机器主动连中转服务器建立隧道，外部流量通过隧道"漏"进内网。开发调试常用 ngrok/frp，生产环境慎用。
