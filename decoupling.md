# 解耦（Decoupling）

## 是什么

解耦是软件工程中降低系统模块间依赖关系的设计思想。**耦合**越高，改一处就要动全身；**解耦**就是让模块各自独立，改动影响最小化。

## 类比：电脑硬件

装一台电脑——CPU、显卡、内存、硬盘各自独立，通过标准化接口（PCIe、SATA、DIMM 插槽）连接。升级显卡时不需要换 CPU，硬盘坏了换一块就好。这就是**物理世界的解耦**。

反之，如果所有部件焊死在一块板上，换任何东西都得整块扔——这就是**紧耦合**。

## 紧耦合 vs 松耦合

```python
# 紧耦合：OrderService 直接依赖具体的 MySQL 实现
class OrderService:
    def __init__(self):
        self.db = MySQLConnection("localhost", "orders_db")  # 写死了 MySQL
    def save(self, order):
        self.db.execute("INSERT INTO orders ...")  # 换数据库 = 重写

# 松耦合：通过接口抽象，依赖倒置
class OrderService:
    def __init__(self, repository: OrderRepository):  # 依赖接口，不依赖具体
        self.repo = repository
    def save(self, order):
        self.repo.save(order)  # 管它存 MySQL 还是 MongoDB
```

## 常见解耦手段

| 手段 | 原理 | 典型场景 |
|------|------|----------|
| **接口抽象** | 依赖接口而非具体实现 | 更换数据库、存储引擎 |
| **依赖注入** | 外部传入依赖，不用自己 new | 测试时注入 Mock |
| **消息队列** | 异步解耦，生产者不直接调用消费者 | 下单后发短信、扣库存 |
| **事件驱动** | 发事件而非直接调用，订阅者自行响应 | 微服务间通信 |
| **微服务** | 按业务边界拆分独立服务 | 大型系统模块化 |
| **配置外置** | 硬编码变配置，改配置而非改代码 | 开关、阈值、地址 |

## 解耦的好处

- **可维护性**：改一个模块不波及全局
- **可测试性**：模块可独立测试（Mock 依赖接口即可）
- **可替换性**：换实现不需要重写调用方
- **独立部署**：微服务各自发布，不互相阻塞
- **团队并行**：解耦后的模块可由不同团队独立开发

## 一句话总结

解耦 = 让模块之间"不认识对方是谁，只知道对方能做什么"。核心手段是面向接口编程、依赖注入和异步消息。紧耦合是软件腐烂的根源之一。
