# HTTP 状态码大全

## 状态码分类

| 类别 | 范围 | 含义 |
|------|------|------|
| **1xx** | 100-103 | 信息响应——收到了，正在处理 |
| **2xx** | 200-226 | 成功——请求已接收并处理 |
| **3xx** | 300-308 | 重定向——需要进一步动作 |
| **4xx** | 400-451 | 客户端错误——你的请求有问题 |
| **5xx** | 500-511 | 服务端错误——服务器炸了 |

## 日常最常用状态码

### 2xx 成功
| 状态码 | 含义 | 场景 |
|--------|------|------|
| **200 OK** | 成功 | GET/POST 正常返回 |
| **201 Created** | 已创建 | POST 创建资源成功后返回 |
| **204 No Content** | 成功但无返回体 | DELETE 成功、保存设置 |
| **206 Partial Content** | 部分内容 | 断点续传、分块下载 |

### 3xx 重定向
| 状态码 | 含义 | 关键差异 |
|--------|------|----------|
| **301 Moved Permanently** | 永久重定向 | 浏览器会**缓存**此跳转，下次直接走新地址 |
| **302 Found** | 临时重定向 | 浏览器**不缓存**，每次都问原地址 |
| **304 Not Modified** | 未修改 | 缓存有效，请用本地缓存（节省带宽） |
| **307/308** | 重定向但**不改变请求方法** | 301/302 可能把 POST 变成 GET，307/308 严格保持 |

> ⚠️ **301 踩坑**：一旦设置 301，浏览器永久缓存。你后来改了跳转规则，用户浏览器也不鸟你。没有十足的"永久"把握，先用 302。

### 4xx 客户端错误
| 状态码 | 含义 | 区别 |
|--------|------|------|
| **400 Bad Request** | 请求格式错误 | 参数解析失败、JSON 格式不对 |
| **401 Unauthorized** | 未认证——"你是谁？" | 没登录/没 token |
| **403 Forbidden** | 无权限——"我知道你是谁，但你不能看" | 已登录但权限不够 |
| **404 Not Found** | 资源不存在 | 也可以用来隐藏资源（不想让人知道 403 的区别） |
| **405 Method Not Allowed** | 请求方法不对 | GET 了只支持 POST 的接口 |
| **422 Unprocessable Entity** | 参数校验失败 | 参数格式对但业务逻辑不通（如年龄=-5） |
| **429 Too Many Requests** | 被限流了 | 等一会儿降低频率重试 |

### 5xx 服务端错误
| 状态码 | 含义 | 排查方向 |
|--------|------|----------|
| **500 Internal Server Error** | 服务端未知错误 | 查日志、看异常堆栈 |
| **502 Bad Gateway** | 网关/代理收到了上游的无效响应 | 上游服务挂了或返回格式不对 |
| **503 Service Unavailable** | 服务暂时不可用 | 过载/维护/重启中 |
| **504 Gateway Timeout** | 网关等上游响应超时 | 上游处理太慢 |

## 非标准状态码（代理/CDN/云平台）

| 状态码 | 来源 | 含义 |
|--------|------|------|
| **499** | Nginx | 客户端主动断开连接（不等服务器响应就走） |
| **520-530** | Cloudflare | 源站连接异常系列 |
| **524** | Cloudflare | 源站响应超时（Cloudflare 等不到源站回复） |
| **460/463** | AWS ELB | 客户端在 ELB 空闲超时前断开连接 |
| **440/449** | IIS | 登录超时（440）/ 需要重试（449） |

## 一句话总结

2xx 一切OK，3xx 去别处找，4xx 你搞错了，5xx 我搞砸了。401 vs 403 是最容易混淆的：401 = 没进门，403 = 进了门但不让你看这个房间。
